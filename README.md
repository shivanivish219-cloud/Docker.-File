# Docker.-File

A minimal static demo containing a single HTML page and supporting assets.

Contents

- `gym.html` — main demo page
- `assets/STYLE.CSS` — styles (and other assets)
- `LICENSE` — project license

Quick preview

- Start a simple HTTP server and open the page:
  - `python -m http.server 8000`
  - Visit http://localhost:8000/gym.html

Notes

- Static files only — no Docker required.
